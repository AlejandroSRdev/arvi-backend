/**
 * Difficulty Enum (Domain Entity)
 *
 * Represents the difficulty levels for habit actions.
 * Domain value object with no external dependencies.
 */

export const Difficulty = Object.freeze({
  LOW: 'baja',
  MEDIUM: 'media',
  HIGH: 'alta',
});

export default Difficulty;
